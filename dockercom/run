sudo df -h

mkdir /tmp/dockercom
cp dockercom/windows10.yml /tmp/dockercom/
sudo docker-compose -f /tmp/dockercom/windows10.yml up


mkdir /tmp/dockercom
cp dockercom/windows11.yml /tmp/dockercom/
sudo docker-compose -f /tmp/dockercom/windows11.yml up